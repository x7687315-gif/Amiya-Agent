# start_assistant.bat 启动失败 · 诊断与修复方案报告

> 生成时间：2026-08-16（续 2026-08-15 会话）
> 触发：真实 Windows 双击 `start_assistant.bat` 后，cmd 报 `'等待' / 'dy' / '坏' is not recognized`，
> 且项目根目录凭空出现 4 个 0 KB 中文 / 乱码文件。
> 状态：**仅诊断 + 方案，未改动任何文件**（按用户要求先不修改）。

---

## 0. 先回答一个问题：为什么要改英文？

**不需要。** “中文模型”指的是运行时的 `Assistant Agent`（Python / Flet / GPT-SoVITS），
它处理中文的能力与这个 `.bat` 启动器用什么编码**完全无关**——启动器只是个薄壳脚本，
负责按顺序拉起 TTS 和 Agent，自身不含任何中文逻辑。

所以：
- 上一轮我建议“改英文”是**过度方案**，属于偷懒绕开问题，已被用户指出不妥。
- 正确做法是**保留中文**，只把 `.bat` 的编码与个别语法修对。下面方案 A 就是“保留中文”的修法。

---

## 1. 现象（用户本机真实复现）

1. cmd 窗口顶部报错：
   ```
   '等待' is not recognized as an internal or external command
   'dy' is not recognized as an internal or external command
   '坏' is not recognized as an internal or external command
   ```
2. `<ASSISTANT_AGENT_DIR>\` 根目录多出 4 个 **0 KB** 文件（git 确认为新增未跟踪）：
   - `Agent（绝不抢跑）`
   - `Agent…`（GBK 乱码版）
   - `明确报错，Agent`
   - `鏄庣‘鎶ラ敊锛孉gent`（GBK 乱码版）

---

## 2. 根因（两个独立因素叠加）

### 因素一：文件是 UTF-8 **无 BOM**，被系统按 GBK 解码

- `cmd.exe` 解析 `.bat` 文件时，用的是**系统 ANSI/OEM 代码页（ACP）**，本机是 GBK（936）。
- 文件本身存的是 **UTF-8 无 BOM**，于是 GBK 把 UTF-8 多字节串解成乱码。
- 乱码字节里恰好出现命令 / 符号，于是：
  - 中文 token 被当成命令执行 → `'等待' / 'dy' / '坏' is not recognized`
  - 文件名被写成 GBK 乱码版（`鏄庣‘鎶ラ敊…`）
- **`chcp 65001 >nul` 没能救场**：`chcp` 只改变**控制台输入输出显示**的代码页，
  不改变“文件已经被 GBK 读进来解析”这件事。等它执行时，文件早已按 GBK 解析完了。

### 因素二：REM 注释行里写了 `->` 箭头，触发了重定向

- `REM` 注释**不会抑制重定向解析**（这是 cmd 的已知怪癖）：
  `REM abc -> def` 仍会被解析为“把 REM 的输出重定向到文件 `def`”。
- 文件第 5、7 行的 `->` 正是真凶：
  - 第 5 行：`REM  严格顺序：TTS -> 等待 ready -> Agent（绝不抢跑）`
    → 创建文件 `Agent（绝不抢跑）`（以及 `等待 ready -` 片段）
  - 第 7 行：`REM    ① 启动超时：等 60s 仍不 ready -> 明确报错，Agent 以纯文字模式启动`
    → 创建文件 `明确报错，Agent`
- 叠加因素一后，这些文件名还被 GBK 二次污染成乱码版。

> 结论：因素一造成“报错 + 乱码文件名”，因素二造成“凭空生成文件”。
> 两者要在修复时**同时**处理，否则只修一个仍会留尾巴。

---

## 3. 待清理的乱码文件（修复后需删除）

| 文件 | 来源行 | 大小 |
|------|--------|------|
| `Agent（绝不抢跑）` | 第 5 行 `->` | 0 KB |
| `Agent…`（GBK 乱码版） | 第 5 行，GBK 二次污染 | 0 KB |
| `明确报错，Agent` | 第 7 行 `->` | 0 KB |
| `鏄庣‘鎶ラ敊锛孉gent`（GBK 乱码版） | 第 7 行，GBK 二次污染 | 0 KB |

---

## 4. 解决方案

### ✅ 方案 A（推荐）：保留中文，修编码 + 修箭头

修改内容（仅 `start_assistant.bat`）：

1. **加 UTF-8 BOM（EF BB BF）**
   - 让 `cmd.exe` 认出文件是 UTF-8，按 UTF-8 正确解码 → 中文不再被当命令，乱码消失。
   - 这是保留中文的关键，比 `chcp` 更根本（`chcp` 只管显示不管解析）。
2. **保留首行 `chcp 65001 >nul`**
   - 作用：让 cmd 控制台把中文 echo 输出**显示**正确（仅显示层）。这一行本身用 `>nul` 是合法重定向，不受影响。
3. **把第 5、7 行的 `->` 箭头换成不会被当成重定向的写法**
   - 用**全角箭头 `→`（U+2192）**替代半角 `->`。全角 `→` 是单字符、不是 ASCII `>`，REM 不会触发重定向。
   - 例：`严格顺序：TTS → 等待 ready → Agent（绝不抢跑）`
   - （也可直接改成文字“到 / then”，但 `→` 最贴近原意且安全。）
4. **删除因素二里提到的 4 个 0 KB 文件。**

预期效果：
- 不再有 `'等待' / 'dy' / '坏' is not recognized` 报错 ✅
- 不再生成任何中文 / 乱码文件 ✅
- 中文注释与 echo 全部保留 ✅
- TTS → 等待 ready → Agent 的启动顺序不变 ✅

验证方式（修复后，在你本机）：
1. 双击 `start_assistant.bat`；
2. 黑色 TTS 窗口出现 `Uvicorn running on http://127.0.0.1:9880` 即 ready；
3. 项目根目录**不再**出现新的中文 / 乱码文件；
4. Agent 起后点气泡 🔊 应出声；
5. 保持黑色 TTS 窗口不关。

### 方案 B（兜底，仅在方案 A 在你 Windows 版本上仍异常时采用）

- 若你的 Windows 对“带 BOM 的批处理首行”有兼容问题（极旧版本会把 BOM 字节误当命令），
  则把 `start_assistant.bat` 整体改写为**纯 ASCII（英文）**注释 / echo。
- 影响范围：**仅启动器脚本本身变英文**，运行时 Agent 依旧全中文，对“中文模型”零影响。
- 这是一个安全但牺牲可读性的兜底，不作为首选。

---

## 5. 本次与上一轮的差异说明

| 项目 | 上一轮判断 | 本轮修正 |
|------|-----------|----------|
| 是否需要改英文 | 建议改英文 | **不需要**，BOM 即可保留中文 |
| `chcp 65001` 的作用 | 当成“修复解析” | 仅管**显示**，不解决解析，故无效 |
| 文件生成的真正原因 | 笼统归为“GBK 误解析” | 精确定位为 **REM 行 `->` 触发重定向**（因素二） |
| 根因数量 | 单因素 | **双因素**：无 BOM + `->` 箭头，需同时修 |

---

## 6. 下一步（待你确认）

- 默认执行 **方案 A**（保留中文、加 BOM、换 `→`、删 4 个文件）。
- 你确认后我再动手修改 + 运行 `@code-check` 四维复查 + 本机验证建议。
- 若你本机 Windows 版本特殊导致 BOM 首行异常，再退到方案 B。

（本报告未修改任何文件；4 个乱码文件仍保留在 `<ASSISTANT_AGENT_DIR>\` 根目录，待修复时一并删除。）
