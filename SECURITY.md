# Security Policy

## 本仓库公开边界（重要）

本仓库**只发布通用架构、代码、测试与示例配置**，不包含任何：

- 密钥 / 凭证（API Key、Token、Secret、OAuth credential 等）
- 真实用户数据、真实对话记录、真实长期记忆
- 真实人格配置中的私人内容（默认人格为可替换的公开示例）
- 个人图片 / 音频 / 壁纸 / 语音模型等私人资产
- 未经确认可再分发的第三方版权素材

配套文件 `.env.example` 中**不含任何真实密钥**（均为占位符）。真实密钥只应放在本地 `.env`（已被 `.gitignore` 忽略），绝不要提交。

## 报告漏洞

如发现安全漏洞，请**不要**在公开 Issue 中披露，而是通过私有渠道联系维护者。

报告时请包含：

- 漏洞描述与复现步骤
- 影响范围（哪些组件 / 版本）
- 建议的缓解措施（如有）

## 密钥轮换

若你曾在本机将真实 `DEEPSEEK_API_KEY` 写入 `.env`：

- 立即到对应服务商后台 **revoke / rotate** 该 Key；
- 旧 Key 绝不进入任何提交或 Issue。

## 第三方依赖

Python 依赖见 `requirements.txt` / `pyproject.toml`。请确认各依赖许可证（多为 MIT / BSD / Apache-2.0）后再分发。不确定许可证的素材请勿纳入本仓库。
